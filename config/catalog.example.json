{
  "note": "Sample catalog modeled on a realistic multi-MCP setup. In production this is auto-generated from each server's tools/list. Schemas are trimmed for readability; cost is estimated from their token footprint.",
  "tools": [
    {
      "name": "confluence.search",
      "server": "confluence-jira",
      "description": "Search Confluence pages with CQL and return matching documents.",
      "keywords": ["confluence", "wiki", "docs", "cql", "page"],
      "latencyMs": 900,
      "risk": 0.1,
      "schema": { "type": "object", "properties": { "cql": { "type": "string", "description": "CQL query" }, "limit": { "type": "integer" } }, "required": ["cql"] }
    },
    {
      "name": "jira.search",
      "server": "confluence-jira",
      "description": "Search Jira issues with JQL; returns tickets, status, assignee.",
      "keywords": ["jira", "ticket", "issue", "jql", "sprint", "bug"],
      "latencyMs": 850,
      "risk": 0.1,
      "schema": { "type": "object", "properties": { "jql": { "type": "string" }, "fields": { "type": "string" }, "limit": { "type": "integer" } }, "required": [] }
    },
    {
      "name": "jira.write",
      "server": "confluence-jira",
      "description": "Create or update a Jira issue, or link two issues.",
      "keywords": ["jira", "create", "update", "ticket", "issue", "write"],
      "latencyMs": 1100,
      "risk": 0.8,
      "schema": { "type": "object", "properties": { "action": { "type": "string", "enum": ["create", "update", "link"] }, "project_key": { "type": "string" }, "summary": { "type": "string" }, "description": { "type": "string" }, "issue_type": { "type": "string" }, "fields": { "type": "object" } }, "required": ["action"] }
    },
    {
      "name": "slack.search",
      "server": "slack",
      "description": "Search Slack messages and files across channels and DMs.",
      "keywords": ["slack", "message", "channel", "dm", "chat", "thread"],
      "latencyMs": 700,
      "risk": 0.2,
      "schema": { "type": "object", "properties": { "query": { "type": "string" }, "count": { "type": "integer" }, "sort": { "type": "string" } }, "required": ["query"] }
    },
    {
      "name": "slack.write",
      "server": "slack",
      "description": "Send a Slack message to a channel or DM.",
      "keywords": ["slack", "send", "post", "message", "notify"],
      "latencyMs": 600,
      "risk": 0.9,
      "schema": { "type": "object", "properties": { "action": { "type": "string" }, "channel": { "type": "string" }, "text": { "type": "string" }, "thread_ts": { "type": "string" } }, "required": ["action", "channel", "text"] }
    },
    {
      "name": "outlook.sendEmail",
      "server": "ms365",
      "description": "Send an email to recipients with subject and HTML body.",
      "keywords": ["email", "outlook", "mail", "send", "message"],
      "latencyMs": 1200,
      "risk": 0.9,
      "schema": { "type": "object", "properties": { "to": { "type": "array", "items": { "type": "string" } }, "cc": { "type": "array" }, "bcc": { "type": "array" }, "subject": { "type": "string" }, "body": { "type": "string" } }, "required": ["to", "subject", "body"] }
    },
    {
      "name": "outlook.calendar",
      "server": "ms365",
      "description": "Read upcoming calendar events and meetings for the next N days.",
      "keywords": ["calendar", "meeting", "event", "schedule", "agenda"],
      "latencyMs": 800,
      "risk": 0.2,
      "schema": { "type": "object", "properties": { "days_ahead": { "type": "integer" } }, "required": [] }
    },
    {
      "name": "figma.getDesignContext",
      "server": "figma",
      "description": "Get design context (code, screenshot, tokens) for a Figma node.",
      "keywords": ["figma", "design", "node", "component", "screenshot", "tokens", "ux"],
      "latencyMs": 2200,
      "risk": 0.1,
      "schema": { "type": "object", "properties": { "nodeId": { "type": "string" }, "clientFrameworks": { "type": "string" }, "clientLanguages": { "type": "string" }, "artifactType": { "type": "string" }, "taskType": { "type": "string" } }, "required": [] }
    },
    {
      "name": "figma.getScreenshot",
      "server": "figma",
      "description": "Render a screenshot of a Figma node or the current selection.",
      "keywords": ["figma", "screenshot", "render", "image", "design", "ux"],
      "latencyMs": 1800,
      "risk": 0.1,
      "schema": { "type": "object", "properties": { "nodeId": { "type": "string" } }, "required": [] }
    },
    {
      "name": "ds.getComponent",
      "server": "design-system",
      "description": "Fetch production TypeScript for a design-system component.",
      "keywords": ["component", "design system", "react", "code", "ux", "button", "cva"],
      "latencyMs": 1400,
      "risk": 0.1,
      "schema": { "type": "object", "properties": { "componentName": { "type": "string" } }, "required": ["componentName"] }
    },
    {
      "name": "ds.getToken",
      "server": "design-system",
      "description": "Fetch a design token (color, spacing, typography, shadow).",
      "keywords": ["token", "color", "spacing", "typography", "design system", "ux"],
      "latencyMs": 500,
      "risk": 0.0,
      "schema": { "type": "object", "properties": { "category": { "type": "string" }, "name": { "type": "string" } }, "required": ["category", "name"] }
    },
    {
      "name": "browserclaw.snapshot",
      "server": "browserclaw",
      "description": "Capture a page's accessibility tree with actionable element refs.",
      "keywords": ["browser", "web", "page", "snapshot", "dom", "accessibility"],
      "latencyMs": 1600,
      "risk": 0.3,
      "schema": { "type": "object", "properties": { "page": { "type": "integer" }, "mode": { "type": "string" }, "depth": { "type": "number" } }, "required": ["page"] }
    },
    {
      "name": "browserclaw.act",
      "server": "browserclaw",
      "description": "Drive a page: click, type, fill, press, select on an element ref.",
      "keywords": ["browser", "click", "type", "fill", "form", "automation", "web"],
      "latencyMs": 1500,
      "risk": 0.5,
      "schema": { "type": "object", "properties": { "page": { "type": "integer" }, "kind": { "type": "string" }, "ref": { "type": "string" }, "text": { "type": "string" }, "value": { "type": "string" }, "fields": { "type": "array" } }, "required": ["page", "kind"] }
    },
    {
      "name": "perplexity.search",
      "server": "perplexity",
      "description": "Ask Perplexity Sonar for an up-to-date, cited web answer.",
      "keywords": ["search", "web", "research", "current", "citation", "internet", "lookup"],
      "latencyMs": 3000,
      "risk": 0.1,
      "schema": { "type": "object", "properties": { "query": { "type": "string" }, "model": { "type": "string" }, "system_prompt": { "type": "string" } }, "required": ["query"] }
    },
    {
      "name": "context7.queryDocs",
      "server": "context7",
      "description": "Fetch current library/framework documentation and code examples.",
      "keywords": ["docs", "library", "framework", "api", "sdk", "reference", "code"],
      "latencyMs": 2000,
      "risk": 0.0,
      "schema": { "type": "object", "properties": { "libraryId": { "type": "string" }, "query": { "type": "string" } }, "required": ["libraryId", "query"] }
    },
    {
      "name": "stitch.generateScreen",
      "server": "stitch",
      "description": "Generate a UI screen design from a text prompt.",
      "keywords": ["ui", "screen", "generate", "design", "mockup", "prototype", "ux"],
      "latencyMs": 8000,
      "risk": 0.2,
      "schema": { "type": "object", "properties": { "projectId": { "type": "string" }, "prompt": { "type": "string" }, "deviceType": { "type": "string" } }, "required": ["projectId", "prompt"] }
    }
  ]
}
